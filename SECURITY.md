# Security Policy · 安全策略

**芯桥（HarnessHub）** 将安全与合规置于首位。本文件定义漏洞上报、
治理红线与防投毒（LLM 供应链投毒）机制。

---

## 治理红线（安全底线）

运行时内置三条强制闸门（命中 → 拦截 + `governance.log` 留痕，**不自动放行**）：

| 闸门 | 名称 | 拦截 |
|------|------|------|
| **G1** | 数据不出服务器 | `password`/`secret`/`api_key`/`private_key` 裸匹配；token **赋值形态**（`token:`/`token=`，防误伤文档名词指称） |
| **G2** | 信号非投资建议 | `买入推荐`/`必涨`/`稳赚`/`保证收益`/`股神`/`包赚` |
| **G3** | 不自动改系统提示/安全策略 | `system prompt`/`系统提示`/`安全策略`/`safety_policy` |

- `writeInsight` 默认 **dry-run**（不自动落盘），交由人工确认。
- `applyImprovement` 默认 **reject**，须显式确认；命中红线强制 reject。
- `loadSkill` 对冻结技能做 **SHA 逐字节校验**，哈希不符一律拒加载。

这些是**不可妥协底线**，任何"放宽以实现功能"的请求都不会被接受。

---

## 漏洞上报（不公开）

发现安全漏洞（越权 / 凭据泄露 / 闸门绕过 / 投毒注入等），**请勿**提交公开
issue 或 PR。请通过**私有渠道**上报，并提供：

1. 漏洞类型与影响面（越权/泄露/注入/绕过哪个闸门）
2. 复现步骤 + Python 版本 + 完整输出
3. 如可行，最小复现样例

外部贡献者：请在仓库 **Security → Report a vulnerability** 提交（GitHub
Security Advisories，私有上报，暂不公开）；或发送至维护者**私有联系邮箱**
（见仓库主页/Commits 作者邮箱，勿走公开 issue）。
内部成员：走既有团队受控通道上报。

我们将在 **1 个工作日**内确认并评估，按危急程度排期修复。

> 披露策略：修复前不公开漏洞细节，修复并发布后统一披露（responsible disclosure）。

---

## 防投毒（LLM 供应链攻击）机制

本项目承载多 Agent 共享的技能库与记忆，**天然是投毒攻击目标**。防御如下：

1. **冻结技能 SHA 锁定**：`vetted/` 冻结技能与 MANIFEST SHA 逐字节比对，
   篡改即拒加载（`loadSkill`）。
2. **PR 双重门禁**（见 [CONTRIBUTING](CONTRIBUTING.md)）：
   - `skill-vetter` 合规审查（冻结一致 + G1/G2/G3 + 风险分级 + 红名单）
   - `buildguard` 防投毒扫描（禁用硬编码 / `~/.openclaw/` 路径）
3. **治理闸门兜底**：即便技能内容混入恶意指令，G1/G2/G3 拦截数据外泄 /
   投资建议 / 系统提示篡改。
4. **不自动落盘**：写记忆 / 应用改进均需人工确认，阻断 silent 投毒写入。

**作为维护者/核心贡献者的责任**：不得以"测试方便"或"功能需要"为由放宽
SHA 校验、闸门或 dry-run 默认安全——这是重建信任的根基（借鉴 08-17
token 正则误伤 → 收紧为赋值形态时仍保留敏感词裸匹配的历史决策）。

---

## 受支持版本

| 版本 | 支持 | 说明 |
|------|:---:|------|
| v0.1.x | ✅ | 当前基线，接受安全修复 |

---

*维护：HarnessHub 核心维护者｜版本 v0.1.0｜变更见 [CHANGELOG.md](CHANGELOG.md)*
